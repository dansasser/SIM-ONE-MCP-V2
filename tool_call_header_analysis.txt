Tool Call Authentication Analysis - 2025-10-19 11:04:52
================================================================

BREAKTHROUGH: Authentication middleware now executes successfully!
PROBLEM DISCOVERED: X-API-Key header NOT being sent with tool calls

TOOL CALL ATTEMPT:
-----------------
Tool: five_laws_validate_text
Timestamp: 2025-10-19 11:04:52.373

AUTHENTICATION MIDDLEWARE LOGS (SUCCESS):
------------------------------------------
[OK] AUTH MIDDLEWARE: on_call_tool triggered
[OK] Tool name: five_laws_validate_text
[OK] Got HTTP request object: <class 'starlette.requests.Request'>
[OK] Extracted headers dict with 5 headers

HEADERS RECEIVED:
-----------------
Total headers: 5
Headers present: ['accept', 'mcp-session-id', 'content-type', 'host', 'content-length']

HEADERS MISSING:
----------------
- X-API-Key: NOT PRESENT
- Authorization: NOT PRESENT

RESULT:
-------
ERROR: [FAIL] No API key found in either header
Message to client: "Missing API key. Provide in Authorization (Bearer) or X-API-Key header"

================================================================

ANALYSIS:

1. MIDDLEWARE FIX WORKS:
   ✓ get_http_request() successfully retrieves Starlette Request object
   ✓ Headers are accessible via dict(request.headers)
   ✓ Middleware executes on tool calls (not just protocol methods)
   ✓ Logging shows complete execution flow

2. NEW PROBLEM IDENTIFIED:
   X Codex NOT sending X-API-Key header with tool calls
   X Only standard HTTP headers present
   X Custom headers from Codex config not forwarded

3. HEADERS THAT ARE PRESENT:
   - accept: Standard HTTP header
   - mcp-session-id: MCP protocol session tracking
   - content-type: Standard HTTP header
   - host: Standard HTTP header
   - content-length: Standard HTTP header

4. HEADERS EXPECTED BUT MISSING:
   - x-api-key: sk_simone_uiDejaAs6WFL3AMS3PWjLOA3Rn7ZZqdk
   - authorization: Bearer <token>

================================================================

CODEX CONFIGURATION (User's config.toml):
------------------------------------------
[mcp_servers.sim-one-mcp]
url = "http://127.0.0.1:8000/mcp"
headers = { X-API-Key = "sk_simone_uiDejaAs6WFL3AMS3PWjLOA3Rn7ZZqdk" }

This configuration SHOULD send X-API-Key header with ALL requests.

================================================================

POSSIBLE CAUSES:

1. MCP Streamable-HTTP Protocol Behavior:
   - Headers might only be sent on INITIAL connection (POST /mcp for initialize)
   - Subsequent tool calls might use session ID instead of re-sending headers
   - Session established on first request, subsequent requests authenticated by session

2. Codex Implementation:
   - Might strip custom headers after session establishment
   - May only send headers for connection, not for each tool call
   - Could be using session-based auth instead of per-request auth

3. MCP Protocol Design:
   - Session ID (mcp-session-id header) might be the intended auth mechanism
   - Custom headers might not be part of the MCP spec for tool calls
   - Protocol might expect session-level authentication, not request-level

================================================================

WHAT THIS MEANS:

The fix we implemented (get_http_request()) works perfectly.
The middleware CAN access headers from tool calls.

BUT: We need a DIFFERENT authentication approach because:
- X-API-Key header is NOT being forwarded to tool calls
- Only session-id is present for subsequent requests
- Current middleware expects per-request authentication

================================================================

POSSIBLE SOLUTIONS:

Option 1: Session-Based Authentication
- Authenticate on INITIAL connection (initialize request)
- Store valid session IDs in memory
- Check mcp-session-id header in middleware instead of X-API-Key

Option 2: Custom MCP Server Implementation
- Override session creation to require API key
- Store API key association with session ID
- Validate session ID maps to valid API key

Option 3: Different Transport Protocol
- Use SSE transport instead of streamable-http
- Check if SSE forwards headers differently

Option 4: Check FastMCP Authentication Patterns
- FastMCP might have built-in session authentication
- Research FastMCP docs for proper auth implementation
- May need to use Auth class instead of Middleware

================================================================

NEXT STEPS:

1. Research MCP streamable-http protocol specification
2. Check if headers are sent on initialize request
3. Research FastMCP authentication patterns
4. Determine if session-based auth is correct approach
5. May need to implement authentication at session creation time

================================================================

STATUS: Need to pivot authentication approach from per-request to session-based.
